//WAP to input and Add two number
# include<stdio.h>
int main(){
  int a,b;
  printf("Enter the First number" );
  scanf("%d",&a );
  printf("Enter the Second number" );
  scanf("%d",&b );
  printf("Sum of a and b is %d", a+b );
  return 0;
}
